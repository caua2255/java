A (Assembly) -> B (Basic) -> C (por isso C) -> C++ -> C#

Python -> "Monty Python"

Java -> "Café das ilhas de Java"

LUA -> porque já existia um linguagem SOL (Simple Object Language)

Java -> Escrever uma vez e rodar em qualquer lugar.

JavaScript tem suporte a orientação a POO.

Prof POO -> Lester GTA 5

Arduino -> Placa de prototipagem que usa o C++

Paradigmas de programação -> formas de organizar o código.
POO foco na criação de objetos/entidade, com atributos/propriedades (variáveis) com ações/funcionalidades (funções/métodos/comportamentos).
-> Variáveis e Funções dentro do (objeto/entidade).
-> Vantagens em comparação com a prog. procedural:
1. Modularidade 
2. Reutilização
3. Software mais flexível (criação)
4. Mapeamento do mundo tangível

Complexidade da POO:
1. Complexidade
2. Desempenho
ea

Paradigma de programação estruturada/procedural -> Paradigma de C:
Lógica de variáveis e Funções.

Pilares da POO:
1. Encapsulamento:
2. Abstração: Tentar generalizar para não engessar o código.
3. Herança: subclasses de uma classe geral/superclasse (que recebem atributos)
4. Polimorfismo:



Classes (formas/molde para criar objetos/blueprint):
-> que gera bolos de diferentes características(atributos/propriedades), mas com a mesma (forma/molde)
Objeto (o que sai da forma) — instância (cópia) de uma classe.

Não existe objeto sem classe, mas existe classe sem objeto (classes abstratas para construção de outras classes "Animal para subclasse mamífero".

















---
porque existe RG e CPF? pq não só um?
























